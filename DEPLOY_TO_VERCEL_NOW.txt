╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║           🚀 DEPLOY TO VERCEL - SIMPLE STEPS                 ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

Follow these steps to deploy your Healthcare Management System to Vercel:

═══════════════════════════════════════════════════════════════

📋 BEFORE YOU START:

1. ✅ You need a Vercel account
   - Go to: https://vercel.com
   - Sign up with GitHub (recommended)

2. ✅ Your MongoDB Atlas should be ready
   - Connection string ready
   - IP whitelist: 0.0.0.0/0 (allow all)

═══════════════════════════════════════════════════════════════

🎯 METHOD 1: EASIEST - Using Vercel Dashboard

STEP 1: DEPLOY BACKEND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go to: https://vercel.com/dashboard
2. Click "Add New..." → "Project"
3. Click "Import Git Repository"
4. Select: nanda-haripriya/HealthCare-FULLSTACK
5. Click "Import"

6. Configure:
   - Project Name: healthcare-backend
   - Root Directory: backend
   - Framework Preset: Other
   - Build Command: (leave empty)
   - Output Directory: (leave empty)
   - Install Command: npm install

7. Add Environment Variables (IMPORTANT!):
   Click "Environment Variables" and add:
   
   Name: MONGO_URI
   Value: mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster.mongodb.net/healthcare
   
   Name: JWT_SECRET
   Value: your-super-secret-jwt-key-12345
   
   Name: PORT
   Value: 5000

8. Click "Deploy"

9. Wait for deployment (2-3 minutes)

10. COPY THE URL! (e.g., https://healthcare-backend-xxx.vercel.app)
    You'll need this for frontend!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 2: DEPLOY FRONTEND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go to: https://vercel.com/dashboard
2. Click "Add New..." → "Project"
3. Click "Import Git Repository"
4. Select: nanda-haripriya/HealthCare-FULLSTACK (same repo)
5. Click "Import"

6. Configure:
   - Project Name: healthcare-frontend
   - Root Directory: frontend
   - Framework Preset: Create React App
   - Build Command: npm run build
   - Output Directory: build
   - Install Command: npm install

7. Add Environment Variable:
   Click "Environment Variables" and add:
   
   Name: REACT_APP_API_BASE_URL
   Value: https://YOUR-BACKEND-URL.vercel.app/api
   
   (Replace YOUR-BACKEND-URL with the URL from Step 1!)

8. Click "Deploy"

9. Wait for deployment (3-5 minutes)

10. YOUR APP IS LIVE! 🎉
    Visit: https://healthcare-frontend-xxx.vercel.app

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 3: UPDATE BACKEND CORS (IMPORTANT!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Open: backend/server.js in your code editor

2. Find the allowedOrigins array (around line 16)

3. Add your frontend URL:
   const allowedOrigins = [
     process.env.FRONTEND_URL,
     "http://localhost:3000",
     "https://healthcare-frontend-xxx.vercel.app", // ADD THIS LINE
   ];

4. Save the file

5. Commit and push:
   git add .
   git commit -m "Add frontend URL to CORS"
   git push

6. Vercel will auto-deploy the backend again!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ VERIFY DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Test Backend:
   Visit: https://YOUR-BACKEND-URL.vercel.app
   Should see: "🏥 HealthCare+ Backend Running ✅"

2. Test Frontend:
   Visit: https://YOUR-FRONTEND-URL.vercel.app
   - Try to signup
   - Try to login
   - Book an appointment
   - Test double-booking feature

═══════════════════════════════════════════════════════════════

🎯 METHOD 2: Using Vercel CLI (Advanced)

If you prefer command line:

1. Install Vercel CLI:
   npm install -g vercel

2. Login:
   vercel login

3. Deploy Backend:
   cd backend
   vercel
   (Follow prompts, add environment variables in dashboard)

4. Deploy Frontend:
   cd frontend
   vercel
   (Follow prompts, add environment variables in dashboard)

═══════════════════════════════════════════════════════════════

🔐 ENVIRONMENT VARIABLES SUMMARY

Backend Environment Variables (in Vercel Dashboard):
┌─────────────┬──────────────────────────────────────────────┐
│ Variable    │ Value                                        │
├─────────────┼──────────────────────────────────────────────┤
│ MONGO_URI   │ mongodb+srv://user:pass@cluster.mongodb.net/ │
│ JWT_SECRET  │ your-secret-key-here                         │
│ PORT        │ 5000                                         │
└─────────────┴──────────────────────────────────────────────┘

Frontend Environment Variables (in Vercel Dashboard):
┌──────────────────────────┬─────────────────────────────────┐
│ Variable                 │ Value                           │
├──────────────────────────┼─────────────────────────────────┤
│ REACT_APP_API_BASE_URL   │ https://backend-url.vercel.app/api │
└──────────────────────────┴─────────────────────────────────┘

═══════════════════════════════════════════════════════════════

🚨 COMMON ISSUES

Issue: "Cannot connect to backend"
Fix: Check REACT_APP_API_BASE_URL is correct in frontend

Issue: "MongoDB connection failed"
Fix: Add 0.0.0.0/0 to MongoDB Atlas IP whitelist

Issue: "CORS error"
Fix: Add frontend URL to backend/server.js allowedOrigins

Issue: "Environment variables not working"
Fix: Redeploy after adding variables in Vercel dashboard

═══════════════════════════════════════════════════════════════

📱 YOUR DEPLOYED URLS

After deployment, you'll have:

Backend:  https://healthcare-backend-xxx.vercel.app
Frontend: https://healthcare-frontend-xxx.vercel.app

Share the frontend URL with users!

═══════════════════════════════════════════════════════════════

📝 IMPORTANT NOTES

1. Vercel auto-deploys on every git push to main branch
2. Free tier has bandwidth limits
3. Keep your environment variables secure
4. MongoDB Atlas must allow Vercel IPs (0.0.0.0/0)

═══════════════════════════════════════════════════════════════

🎊 THAT'S IT!

Your Healthcare Management System is now live on Vercel!

For detailed instructions, see: VERCEL_DEPLOYMENT_GUIDE.md

═══════════════════════════════════════════════════════════════
